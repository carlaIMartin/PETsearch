@model Vets

@{
    ViewData["Title"] = "Login";
}
<div class="row">
    <div class="col-md-6 offset-3">
        <p>
            <h4>Log in to your account</h4>
        </p>
        @if (TempData["Error"] != null)
        {
            <div class="col-md-12 alert alert-danger">
                <span><b>Sorry!</b> - @TempData["Error"]</span>
            </div>
        }
        <div class="row">
            <div class="col-md-8 offset-2">
                <form asp-action="Login">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="Vet_Username" class="control-label"></label>
                        <input asp-for="Vet_Username" class="form-control">
                        <span asp-validation-for="Vet_Username" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Vet_Password" class="control-label"></label>
                        <input asp-for="Vet_Password" class="form-control" />
                        <span asp-validation-for="Vet_Password" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input class="btn btn-outline-success float right" type="submit" value="Log in" />
                        <a class="btn btn-outline-secondary" asp-controller="Vets" asp-action="Login">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>